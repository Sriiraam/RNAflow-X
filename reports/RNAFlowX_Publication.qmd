{`---
title: ""
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    page-layout: full
    theme: cosmo
    css: ../theme.scss
    embed-resources: true

execute:
  echo: false
  warning: false
  message: false
---

```{r}
#| include: false

library(knitr)
library(dplyr)
library(kableExtra)

results_dir <- "../results"

de <- read.csv(
  file.path(
    results_dir,
    "differential_expression",
    "deseq2_results",
    "differential_expression.csv"
  ),
  stringsAsFactors = FALSE
)

mapped <- read.csv(
  file.path(
    results_dir,
    "functional_enrichment",
    "enrichment_results",
    "annotation",
    "mapped_DESeq2_results.csv"
  ),
  stringsAsFactors = FALSE
)

summary_lines <- readLines(
  file.path(
    results_dir,
    "functional_enrichment",
    "enrichment_results",
    "enrichment_summary.txt"
  )
)

get_summary_value <- function(label) {
  hit <- grep(
    paste0("^", label, ":"),
    summary_lines,
    value = TRUE
  )

  if (length(hit) == 0) {
    return(NA_integer_)
  }

  as.integer(
    trimws(
      sub(
        paste0("^", label, ":"),
        "",
        hit[1]
      )
    )
  )
}

de_genes <- nrow(de)
mapped_genes <- nrow(mapped)

sig <- !is.na(de$padj) &
  de$padj < 0.05 &
  abs(de$log2FoldChange) >= 1

significant_genes <- sum(sig)
up_genes <- sum(sig & de$log2FoldChange >= 1)
down_genes <- sum(sig & de$log2FoldChange <= -1)

gsea_genes <- get_summary_value("GSEA ranked genes")

annotation_success <- 100 * mapped_genes / de_genes
```

<div class="hero">

# RNAFlowX

### A Reproducible Bulk RNA-seq Analysis Pipeline

**Author:** Sriram B

**Version:** 1.1.0

**Generated:** `r format(Sys.Date(), '%d %B %Y')`

*Nextflow • DESeq2 • Salmon • clusterProfiler • Quarto*

</div>

# Executive Summary

RNAFlowX is a reproducible transcriptomic analysis workflow developed for **Bulk RNA sequencing**. The pipeline performs read quality assessment, transcript quantification, differential expression analysis, Gene Ontology enrichment, KEGG pathway discovery and Gene Set Enrichment Analysis (GSEA) within a fully reproducible Nextflow framework.

<div class="metrics">

<div class="card">
<h2>`r format(de_genes, big.mark=",")`</h2>
<p>Total Genes</p>
</div>

<div class="card">
<h2>`r format(significant_genes, big.mark=",")`</h2>
<p>Significant DEGs</p>
</div>

<div class="card">
<h2>`r format(up_genes, big.mark=",")`</h2>
<p>Upregulated</p>
</div>

<div class="card">
<h2>`r format(down_genes, big.mark=",")`</h2>
<p>Downregulated</p>
</div>

<div class="card">
<h2>`r format(mapped_genes, big.mark=",")`</h2>
<p>Mapped Genes</p>
</div>

<div class="card">
<h2>`r sprintf("%.1f%%", annotation_success)`</h2>
<p>Annotation Success</p>
</div>

</div>

::: {.section}
## Project Overview

**Objective**

Identify transcriptional alterations induced by PFOS exposure using a reproducible RNA-seq workflow.

**Organism:** Homo sapiens

**Quantification:** Salmon

**Differential expression:** DESeq2

**Functional analysis:** GO, KEGG, GSEA

**Report generation:** Quarto HTML
:::

::: {.section}
## RNAFlowX Workflow

| Stage | Tool | Output |
|---|---|---|
| Quality Control | FastQC + MultiQC | QC metrics |
| Preprocessing | FastP | Clean FASTQ |
| Quantification | Salmon | Transcript abundance |
| Counting | Tximport | Gene count matrix |
| Differential Expression | DESeq2 | DEG table |
| Functional Analysis | clusterProfiler | GO / KEGG |
| Gene Set Analysis | GSEA | Enrichment profiles |
| Reporting | Quarto | Interactive HTML |
:::

> **Scientific objective:** This publication summarizes the complete downstream transcriptomic analysis while preserving reproducibility and minimizing command-line content.`}
